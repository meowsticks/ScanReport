node_modules/
*.html
!ScanReport-playground.html
# Per-page PNG previews are regenerable from the build scripts — keep the
# committed PDF as the source of truth, not the screenshots.
ScanReport-midjob-p*.png
testhtml-*.png
# Header/divider decision renders — regenerable from build-options.mjs
header-*.png
div-*.png
